package main

import (
	"encoding/json"
	"fmt"
	"os"
	"time"
)

var (
	_ = json.RawMessage{}
	_ = fmt.Fprintf
	_ = os.Stderr
)

func GetCPUTime() int64 {
	return time.Now().UnixNano()
}

// The user's code will be inserted below here.
